package lib;

import java.io.File;
import java.io.FileInputStream;



public class ExcelDataFile {
	
/*	File file;
	FileInputStream fis;
	XSSFWorkbook wb;
	public XSSFSheet sheet;
	public ExcelDataFile(String path)
	{
	
		try{
		this.file=new File(path);
		fis=new FileInputStream(file);
		wb=new XSSFWorkbook(fis);
		}
		catch(Exception exe)
		{
			exe.printStackTrace();
		}
		
		
		
	}
	
	
	public String getExcelData(int sheetnum,int rownum,int celnum )
	{
		
		sheet=wb.getSheetAt(sheetnum);
		int cel=sheet.getRow(rownum).getLastCellNum();
		int row;
		if(cel<celnum||sheet.getLastRowNum()<rownum)
		{
			String data=sheet.getRow(rownum).getCell(celnum).getStringCellValue();
			return data;
		}
		
		else
			return "not found";
		
		
		
		
		  
	}
	*/
	

}
